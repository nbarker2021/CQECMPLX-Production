# Lib-Forge Package Split

Status: `package_family_split_started`

`CQECMPLX-LibForge-Lattice-ReForge-Ring` is now split into named package
families for later promotion.

## Primary Families

| Family | Status | Route Note |
|---|---|---|
| `LatticeForge-Core` | `primary_package_candidate` | split source from ledger payload |
| `Kernel-Ring` | `primary_handshake_candidate` | bind handshakes to kernel/adapters |
| `ChromaForge` | `package_candidate` | review MMDB and receipt boundaries |
| `GraphStax` | `package_candidate` | graph/permutation role binding needed |
| `PixelForge` | `package_candidate` | resolve duplicate source roots |
| `CQE-Engine-And-Shared-Memory` | `core_library_candidate` | compare Production and AirLock variants |
| `MetaMaterial-Designer` | `tracked_elsewhere` | see its source binding |
| `Odysseus-MCP-Memory` | `tracked_elsewhere` | see its source binding |
| `Analog-Forge-Workbench` | `tracked_elsewhere` | see its source binding |
| `Papers-And-Summaries` | `publication_material` | route through paper/publication manifests |

## Immediate Next Gates

1. Split `LatticeForge-Core` into source and ledger payload records.
2. Bind `Kernel-Ring` handshakes to the kernel runtime.
3. Compare `CQE-Engine-And-Shared-Memory` between Production and AirLock.
4. Keep generated paper/PDF output out of package source promotion.
