# CQECMPLX-LatticeForge-ReForge

Status: `trackable-composite`

## Identity

This composite gathers the Forge-family material around LatticeForge, ReForge,
Forge package naming, lattice proof geometry, and reusable library behavior.

It is not a choice between LatticeForge and ReForge. It is the composite family
that absorbs every source saying the same thing about Forge-style package
construction.

## Current Evidence

- `CMPLX-PartsFactory-main (1).zip` is content-profile-complete.
- PartsFactory top claim terms include `lattice`, `forge`, `receipt`,
  `compose`, `api`, `proof`, `claim`, and `docker`.
- PartsFactory has a queued payload:

```text
packages/lattice-forge/src/lattice_forge/ledger/data/cmplx_morphism_ledger_seed_v0_6.db
```

- `Lib Kernel`, `Lattice Kernel`, and `ReForge Kernel` deployment proofs are
  already slice-ready.
- R30 and Formalization both bridge back to PartsFactory and lattice-forge
  proof/formalization paths.

## Composite Rule

Source names stay as evidence. The production composite name becomes:

```text
CQECMPLX-LatticeForge-ReForge
```

The composite should include:

- LatticeForge package identity;
- ReForge umbrella identity;
- Forge-family naming rule;
- lattice proof geometry;
- morphism ledger payload route;
- Docker/Compose/API/MCP adapter evidence;
- proof/formalization bridge to R30 and Formalization.

## Not Yet Done

- Expand/introspect the morphism ledger DB.
- Bind exact source files to paper claims.
- Produce the production package/API shape.
