# CQE-E/G8 Datum Decision Rules

Gate outputs are library-bound by default.

Create a new datum only when an accepted gate result introduces irreducible content not representable as a receipt, morphon, diagnostic trace, or adapter output.
