# CMPLX-MORSR Open Obligations

- Bind the final supplied MORSR implementation.
- Replace placeholder/generalized E8 direction generation with canonical E8 roots if the supplied tool contains them.
- Decide whether `pode` and `node` are distinct record types or one record with aliases.
- Add receipt persistence once the final MORSR trace schema is supplied.
- Add contamination detection for hidden-result diagnostics.
- Add a regression pack proving every non-math diagnostic hides the result until after guess commit.
- Add cross-port examples for `engine`, `diagnostic`, `receipt`, and `memory`.
