"""PartsFactory v0.3 variants of diverged lattice_forge modules.

Preserved for adjudication — import explicitly, e.g.:
    from lattice_forge.variants_partsfactory import core as core_pf
The PartsFactory __init__ variant is stored as `_init_variant.py` (it expects
the full PartsFactory sibling set and must not auto-run here).
"""
