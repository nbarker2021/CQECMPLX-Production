# Production Slices

This directory holds curated production-shaped content that has passed a
promotion manifest.

Current slice:

- `PROMOTION_SLICE_2026-06-11_PAPER_PROOF_TEXT.md`

## Rule

Only manifest-bound slices belong here. Raw workspaces, archives, caches,
virtual environments, database payloads, and generated binaries stay out unless
a later promotion manifest explicitly allows them.
